package templates

import "tom/internal/styles"

templ Bistro() {
	<div id="content" class={ content(styles.Pallete()) }>
		Welcome to the Theatre's Bistro,
		a place where you can grab a meal and a drink 
		(or at least a recipe) and enjoy life.
	</div>
}
